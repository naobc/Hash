Este código lee los nombres desde un archivo .txt,guarda los nombres en HashSet(elimina duplicados y los almacena sin orden),hashSet se convierte en una lista,
ordena usando Collections.sort y por ultimo imprime un archivo resultado.txt con el ordenamiento alfabetico de los nombres.
